{
  "name": "pfetest",
  "version": "1.0.0",
  "description": "git clone https://github.com/AmineMlaiel/PfeFinal",
  "main": "index.js",
  "scripts": {
     "dev": "concurrently \"cd backend && set NODE_ENV=development && nodemon\" \"cd frontend && set NODE_ENV=development && ng s\"",
  "start": "concurrently \"cd backend && set NODE_ENV=production && node server.js\" \"cd frontend && set NODE_ENV=production && ng s\""
,
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.2",
    "express-fileupload": "^1.5.1"
  }
}
